<!-- @generated by `just model-write` from model/ids.toml. -->
<!-- Do not edit: model/*.toml is the source of truth. -->

# CONFORMANCE

Every registered id has exactly one Gherkin scenario and exactly one executable test named `conformance_<id>`.

| ID | Level | Suite | Statement |
| --- | --- | --- | --- |
| `SI-01` | `build` | `site` | The homepage contains no hyphen or dash in any visible text, code samples included. |
| `SI-02` | `build` | `site` | The homepage headline is exactly Free verified AI inference. |
| `SI-03` | `build` | `site` | The homepage prose is under 120 words, code samples excluded. |
| `RC-01` | `build` | `receipts` | A stock OpenAI client receives a system fingerprint equal to the resolved model kappa joined with the engine kappa. |
| `RC-02` | `build` | `receipts` | The receipt named in the response header resolves to a stored object whose kappa matches its bytes and whose signature verifies. |
| `RC-03` | `build` | `receipts` | A response for a model that does not resolve in the catalog carries no fingerprint and no receipt header. |
| `RC-04` | `build` | `receipts` | Every module registers and the merged router boots without a route collision. |

## Claims that are not conformance ids

| ID | Level | Statement | Authority |
| --- | --- | --- | --- |
| `AUTH-BLAKE3` | `some-true` | BLAKE3 is a cryptographic hash whose output is stable across platforms for identical input bytes. | https://github.com/BLAKE3-team/BLAKE3-specs |
| `AUTH-ED25519` | `some-true` | Ed25519 signatures verify deterministically under RFC 8032. | https://www.rfc-editor.org/rfc/rfc8032 |
| `AUTH-OPENAI-FINGERPRINT` | `some-true` | The OpenAI chat completion object defines an optional system_fingerprint string and usage.prompt_tokens_details.cached_tokens. | https://github.com/openai/openai-openapi |

## Evidence levels

- `some-true`: a fact reproduced from an authority. Not established here.
- `build`: constructed here and validated against its oracle. Evidence, not a proof.
- `open`: measured and reported. Never asserted.
